# Extension step — build progress log

The extension build loop (`scripts/ralph-loop.sh`) appends one line here
per completed task in `docs/extension-plan.md`.

- Levels: added `level` field to `Game`, wave-clear now increments it before `spawnAsteroidWave()`, which applies the per-level count and speed-multiplier formulas to spawned asteroids (also applied on split); `level` added to `getState()`. `src/game.js`.
- Saucer enemy: added `saucer`/`saucerBullets` state, a fixed spawn timer active only when `level >= 2` and no saucer is present, straight-line movement with screen wrap, and a fixed-timer bullet fired at the ship's current position (same speed/lifetime as ship bullets); both added to `getState()`. `src/game.js`.
- Saucer collisions and scoring: added bullet-to-saucer collision (destroys saucer, adds `SAUCER_SCORE`), and ship collisions with the saucer and saucer bullets that cost one life via `handleShipHit()`, using the same invulnerability check as the existing ship-to-asteroid collision. `src/game.js`.
- Power-ups: `splitAsteroid()` now rolls `POWERUP_DROP_CHANCE` on every asteroid destroy event and, on success, calls new `spawnPowerup()` to place a power-up (random type: shield, rapidFire, extraLife) at the asteroid's last position with a fixed lifetime; `tick()` counts down and removes expired power-ups; `powerups` added to `getState()`. `src/game.js`.
- Power-up effects and pickup: added ship-to-power-up collision (pickup ignores invulnerability) that calls new `applyPowerup()`; shield sets `ship.timedPowerup` and reuses `invulnerableUntil` for a fixed window, rapid fire sets `ship.timedPowerup` and gates a new tick-based fire cooldown (`SHIP_FIRE_COOLDOWN_TICKS` vs shorter `SHIP_RAPID_FIRE_COOLDOWN_TICKS`), extra life adds 1 life immediately; a same-type pickup resets the timer, a different timed type replaces it (ends shield early if rapid fire is picked up mid-shield); `activePowerup` (type + ticksLeft) added to `getState()`. `src/game.js`.
- Client rendering and HUD: added `drawSaucer`, `drawSaucerBullets`, and `drawPowerups` (labelled ring per type) to the render pipeline, and extended `drawHud` to show the level number and an active power-up indicator with seconds remaining; no physics/collision/timer/drop-chance logic added client-side. `public/game.js`.
- Docker verification and running instructions: confirmed the Dockerfile still needs no changes (extension code lives in the already-copied `server.js`/`src`/`public`, `package.json` gained no new dependencies), so no extra setup step is needed; added an extension playtest checklist (level 2, saucer appearance/movement/fire, saucer destroy and scoring, saucer/bullet hit, and each of the 3 power-up types) to `running.md`.
- Orchestrator verification (outside the loop, after all 7 tasks were checked off): ran `node -c` on the changed files; ran the level/speed formulas, level-up-on-clear, saucer spawn, and power-up spawn directly against the `Game` class in isolation and confirmed the expected values; started the server directly and confirmed `/health`, `/`, and `/game.js`; ran an actual `docker build` and `docker run -p <port>:3000` on the image and confirmed the same endpoints inside the container. Did not click through a full manual browser playtest.
