# src/

Server-side modules for the authoritative game loop (state, physics,
collisions, WebSocket protocol) live here. `server.js` at the repo root
wires them together. Populated during the base build stage per
`docs/base-plan.md`.
