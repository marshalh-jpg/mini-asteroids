{
  "name": "mini-asteroids",
  "version": "0.1.0",
  "description": "Server-authoritative Asteroids: the game loop, physics, and state live in a Dockerized Node server; the browser client only renders and forwards input.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.17.0"
  }
}
